import {makeStyles} from '@material-ui/core'

const useStyles = makeStyles((theme) => ({
    grow: {
      flexGrow: 1,
    },
    layoutTitle:{
      padding:10
    },
    container:{
      height:'100%'
    }
  }));


  export default useStyles;